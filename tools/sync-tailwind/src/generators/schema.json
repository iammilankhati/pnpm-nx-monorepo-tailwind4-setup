{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "$id": "SyncTailwindSources",
  "title": "Sync Tailwind Sources",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the app to sync tailwind sources for (optional when run as sync generator)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which app would you like to sync tailwind sources for?"
    }
  },
  "required": []
}
